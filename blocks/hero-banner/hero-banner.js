export default function decorate() {
  // The hero-banner block relies on default content decoration:
  // the section/block auto-blocking moves the H1 and picture into place,
  // and CSS positions the background image behind the text content.
}
